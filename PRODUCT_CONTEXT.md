# 致地 AI 桌面端原型：产品上下文

## 产品定位

致地 AI 是面向自然资源、国土空间规划与 GIS 专业工作的 AI+GIS 桌面端产品原型。产品以地图为核心工作画布，将自然语言对话、专业空间分析工具、领域知识库和可配置智能体整合到同一工作台，帮助用户完成数据查询、空间分析、合规审查、制图和报告生成等任务。

当前版本用于产品交互与视觉方案演示，不包含真实账号体系、持久化存储或完整后端服务。高德地图使用占位 Key；未配置时展示既有的地图兜底画面。

## 页面模块说明

### 登录视图

- 账号密码登录与手机验证码登录 Tab。
- 品牌介绍、能力卡片、第三方登录和注册入口均为原型展示。
- 当前默认隐藏，工作台默认显示；不要擅自调整这一原型展示状态。

### 应用外壳

- 顶部栏：品牌、面包屑和当前用户。
- 左侧栏：一级导航、会话记录、用户与设置入口。
- 左侧栏支持收缩和拖拽调宽；右侧对话区支持拖拽调宽、收起和全屏。
- 页面切换统一通过 `switchPage(pageId)` 完成。

### GIS 工作台

- 中部为地图、搜索框、地图工具条和任务进度抽屉。
- 地图优先初始化高德 JS API；缺少有效 Key 时保留演示兜底图。
- 右侧为 AI 对话区，包含快捷问题、当前智能体信息、输入框、模型/权限展示和扩展菜单。
- “工作空间”入口模拟桌面端文件夹选择，固定返回 `E:/GIS/project/`，并扫描生成建筑、道路、河流和土地利用四个矢量数据条目。
- 工作空间路径和矢量文件状态由 `scripts/gis-workspace.js` 管理；图层选择、Leaflet 模拟加载与显隐状态由 `scripts/map-layer.js` 管理。

### 工具中心

- 工具箱主体是原子工具广场，负责发现、浏览和获取原子工具。
- 原子工具广场的检索区由搜索框、业务类型筛选和工具类型筛选组成；三类条件组合生效。
- 业务类型当前包括空间分析、数据处理、遥感影像和可视化工具；工具类型包括 MCP 和 Skill。
- “管理原子工具”弹窗负责管理用户已经添加或接入的全部原子工具。
- 管理弹窗一级 Tab 按来源划分为“全部工具 / 广场工具 / 我的工具”；Skill、MCP 是工具类型，只用于二级筛选。
- “我的工具”包含用户上传的 Skill、接入的 MCP，以及未来可能增加的自定义工具。
- 工具列表由 `scripts/tools.js` 中的数据和渲染函数维护。

工具管理数据字段约定：

- `source`：工具来源，`square` 表示从原子工具广场获取，`mine` 表示用户上传、接入或自定义。
- `type`：工具类型，当前为 `skill` 或 `mcp`。
- `enabled`：工具启停状态，是“已启用 / 已停用”筛选的唯一依据。
- `configStatus`：MCP 配置校验状态，仅用于展示“配置成功 / 配置失败”，不能替代启停状态。

### 智能体中心

- 包含“我的智能体”和“智能体广场”两个 Tab。
- 支持搜索、分类筛选、详情、编辑、发布/下架、删除、试用和调用等原型交互。
- 数据与卡片渲染集中在 `scripts/agents.js`。

### 知识库管理

- 主页展示知识分类、热门标签和最近更新通知。
- 详情视图展示分类内知识库列表、搜索、排序和分页信息。
- 数据与渲染集中在 `scripts/knowledge.js`。

### 系统设置与通知

- 设置页保留账号安全、外观、语言、地图、模型等分类入口。
- 通知页包含全部、未读、@我的和系统分类。
- 用户菜单、设置和通知交互集中在 `scripts/account.js`。

### 保留但未接入的原型模块

- `styles/sessions.css` 与 `styles/tasks.css`、`scripts/tasks.js` 是原单文件中保留的会话管理/任务监控样式与逻辑。
- 当前 `index.html` 没有对应的独立页面 DOM，也没有一级导航入口。除非后续需求明确恢复这些页面，否则不要删除、扩展或接入它们。

## 目录与职责

```text
index.html                 页面结构与模块容器
styles/main.css            样式入口及层叠顺序
styles/*.css               按页面/业务域拆分的样式
scripts/core.js            通用方法、登录、导航、侧栏和拖拽
scripts/tools.js           工具中心
scripts/knowledge.js       知识库
scripts/account.js         用户菜单、通知和设置
scripts/tasks.js           保留的任务监控逻辑
scripts/workspace.js       右侧对话面板收起/展开
scripts/map.js             地图初始化与地图工具
scripts/chat.js            AI 对话、扩展菜单和任务抽屉
scripts/agents.js          智能体中心与智能体模态框
scripts/bootstrap.js       首次数据渲染
assets/images/             独立图片资源
reference/                 重构前基线备份，仅用于回归对照
```

## 当前设计规则

- 这是桌面端、全视口原型。`body` 不滚动，各业务面板自行管理滚动区域。
- 视觉风格保持浅色渐变背景、半透明毛玻璃面板、轻阴影和 10–16px 圆角。
- 核心色由 `styles/base.css` 的 CSS 变量控制：绿色为主色、蓝色为辅助色、青色为高亮色。
- 页面标题使用深色，正文使用中性灰；错误、链接和状态色沿用现有变量与状态类。
- 左侧导航、地图工作区和右侧对话区是主工作台的稳定三段式布局。
- Emoji 目前承担品牌、导航和业务图标角色；在没有明确设计需求时不要整体替换图标体系。
- 保留现有中文文案、class/id 命名、DOM 层级和交互反馈，不做视觉重设计。
- `styles/main.css` 中的导入顺序等同于原单文件的 CSS 层叠顺序，调整顺序前必须检查覆盖关系。
- `index.html` 底部脚本顺序体现模块依赖。项目使用经典脚本的共享全局作用域，不要随意改为 ES Module 或调整顺序。

## 后续修改约束

1. 不引入 React、Vue、打包器或新的 UI 框架；保持原生 HTML/CSS/JavaScript 和静态文件可运行。
2. HTML 负责固定页面结构；CSS 负责静态视觉；JavaScript 负责事件、状态和数据驱动的重复内容渲染。
3. 新页面应在 `index.html` 增加独立 `.page` 容器，并在对应 CSS/JS 业务模块中实现，不把大段固定结构拼进 JavaScript。
4. 新图片放入 `assets/images/`，使用相对路径引用；不要重新放回 HTML 的 Data URL 或根目录。
5. 优先复用现有 CSS 变量、按钮、卡片、状态和表单样式。新增 class 应表达业务或组件语义。
6. 不重命名现有 class/id，除非同步检查全部 HTML、CSS、JavaScript 引用并完成回归验证。
7. 静态事件使用 JavaScript 绑定或 `data-*` 动作标记，不在 HTML 中新增 `onclick` 等内联脚本。
8. 运行时数值样式（例如进度宽度、数据颜色）可以由 JavaScript 设置；固定样式必须写入对应 CSS 文件。
9. 高德地图 Key 属于部署配置，不应直接提交真实密钥。地图失败时必须继续保留兜底视图。
10. 每次修改至少检查：控制台无新增错误、一级导航可切换、侧栏/对话区可收展、地图兜底可见、对话可发送、工具/智能体/知识库可渲染。

## 本地查看

直接打开 `index.html` 即可查看静态原型。若浏览器限制本地资源或地图脚本，使用任意静态文件服务器从项目根目录启动；项目不依赖安装或构建步骤。
